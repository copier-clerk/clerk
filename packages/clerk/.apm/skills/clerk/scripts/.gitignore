# The clerk/ subdirectory is generated by `just vendor` from src/clerk/*.py.
# Never commit Python files here — they are derived from src/clerk and drift-checked.
# Run `just vendor` before `apm pack` (or `just pack`/`just release`).
clerk/*.py
