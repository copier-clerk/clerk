# cog.toml — cocogitto monorepo release config (spec 008b / ADR-0006)
#
# MANDATORY (contracts/fanout.md):
#   generate_mono_repository_global_tag = false  — no umbrella PEP 440 tag in the
#     monorepo; only per-module <name>-vX.Y.Z tags are created.
#   tag_prefix = "v"  — combined with the module name separator yields <name>-vX.Y.Z.

generate_mono_repository_global_tag = false
tag_prefix = "v"

# pre_bump rolls back on failure (ADR-0006) — safe for preflight checks.
# spec 009 Phase 0 landed real modules under templates/, so check-modules is now
# a real gate (not the empty no-op) and the `|| true` guard is dropped (SC-008).
pre_bump_hooks = ["just check-modules"]

# Package entries added here by `just new-module <name>`.
# Format: [monorepo.packages.<name>]  path = "templates/<name>"
[monorepo.packages]

[monorepo.packages.clerk-mod-base]
path = "templates/clerk-mod-base"

[monorepo.packages.clerk-mod-python]
path = "templates/clerk-mod-python"

[monorepo.packages.clerk-mod-apm]
path = "templates/clerk-mod-apm"

[monorepo.packages.clerk-mod-precommit]
path = "templates/clerk-mod-precommit"

[monorepo.packages.clerk-mod-ts]
path = "templates/clerk-mod-ts"
[monorepo.packages.clerk-mod-go]
path = "templates/clerk-mod-go"
[monorepo.packages.clerk-mod-rust]
path = "templates/clerk-mod-rust"
[monorepo.packages.clerk-mod-quality]
path = "templates/clerk-mod-quality"
[monorepo.packages.clerk-mod-readme]
path = "templates/clerk-mod-readme"
[monorepo.packages.clerk-mod-agentic]
path = "templates/clerk-mod-agentic"
[monorepo.packages.clerk-mod-ci-github]
path = "templates/clerk-mod-ci-github"
[monorepo.packages.clerk-mod-ci-gitlab]
path = "templates/clerk-mod-ci-gitlab"
[monorepo.packages.clerk-mod-package-add]
path = "templates/clerk-mod-package-add"
[monorepo.packages.clerk-mod-terraform]
path = "templates/clerk-mod-terraform"
[monorepo.packages.clerk-mod-cdk]
path = "templates/clerk-mod-cdk"
