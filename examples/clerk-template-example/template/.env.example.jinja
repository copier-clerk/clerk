# Runtime secrets for {{ project_name }}
#
# Copy this file to .env and fill in real values.  Never commit .env.
# These secrets are needed at RUNTIME — they are NOT copier answers and
# were NOT generated by clerk.  See README.md for details.
#
# Example entries — replace with your project's actual secrets:
# MY_API_KEY=your-api-key-here
# DATABASE_URL=postgresql://user:password@host/db
