# Managed by copier — do NOT edit by hand.
#
# This file records the template source (`_src_path`), the exact version
# (`_commit`), and every answer, so bailiff can reproduce this project faithfully
# with no agent. copier writes it; bailiff reads it at reproduce time.
{{ _copier_answers|to_nice_yaml }}
