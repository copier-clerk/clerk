---
paths:
  - "**/*"
---

Prefer modern CLI tools: rg over grep, fd over find, eza over ls, bat over cat,
pnpm over npm/yarn, uv over pip/poetry, mise over nvm/pyenv, just/task over make.

Dependencies: add via the package manager CLI (not manifest edits), defaulting
to the latest compatible version.

For stack, tooling, framework, orchestration, quality, or observability work,
read [toolchain defaults](../../apm_modules/srobroek/agentic-packages/packages/steering-toolchain-defaults/.apm/context/toolchain-defaults.toolchain-defaults-index.context.md).
