{% if _external_data.precommit.hook_manager != 'none' %}# .pre-commit.d/bailiff-mod-api.yaml — managed by bailiff-mod-api; do not edit by hand.
# Spectral-lint hook fragment; merged into .pre-commit-config.yaml by bailiff-mod-precommit.
repos:
  - repo: local
    hooks:
      - id: spectral-lint
        name: spectral lint (OpenAPI)
        entry: spectral lint openapi.yaml
        language: system
        files: ^openapi\.yaml$
        pass_filenames: false
{% endif %}
