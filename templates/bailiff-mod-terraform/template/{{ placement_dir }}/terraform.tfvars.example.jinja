{#-
  terraform.tfvars.example — SEED-ONCE (_skip_if_exists in copier.yml).

  Example variable values. Copy to terraform.tfvars (gitignored if it contains
  secrets) and fill in real values before applying.
-#}
# terraform.tfvars.example — copy to terraform.tfvars and set real values.
# Do NOT commit terraform.tfvars if it contains secrets.

# environment = "dev"
