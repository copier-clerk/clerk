{#-
  AGENTS.md — SEED-ONCE (_skip_if_exists in copier.yml).

  Ports agents-md/templates/single.md + monorepo.md (chosen by `layout`), with
  PROJECT_NAME/ORG substituted from answers and the description placeholder
  filled from `description` (faithful to agents-md/module.py `_render`).

  The `## Architecture` sentinel span is filled from the FROZEN `architecture_md`
  answer ONLY when `write_architecture=true` (Q3+Q5); otherwise the empty
  sentinel pair is left for a later agent fill. The sentinel markers are kept
  VERBATIM from agents-md (BEGIN_SENTINEL/END_SENTINEL) so future tooling can
  find the span.

  branch_strategy is substituted as a single guidance line (contract §2).
-#}
# {{ project_name }}

{% if description %}{{ description }}{% else %}<!-- PROJECT DESCRIPTION: to be filled by agent -->{% endif %}

## Agent Guidance

<!-- CODEX/AGENTS GUIDANCE: to be filled by agent -->

## AGENTS Layering

- This root `AGENTS.md` applies to the whole repository unless a deeper file overrides it.
- Put repo-wide workflow, architecture, tool, and source-of-truth guidance here.
- Add nested `AGENTS.md` files only for subtrees that need materially different rules.
- Prefer subtree placement over invented path metadata.

## Architecture

<!-- BEGIN ps:architecture -->
{% if write_architecture and architecture_md %}{{ architecture_md }}
{% endif %}<!-- END ps:architecture -->
{% if layout == 'monorepo' %}
## Monorepo Structure

| Path | Contents |
|------|----------|
| `apps/` | User-facing app surfaces |
| `services/` | Long-lived backend deployables |
| `functions/` | Serverless handlers, nested by platform |
| `workers/` | Background jobs and consumers, nested by platform |
| `libs/` | Internal shared code by architectural role |
| `packages/` | Published or independently versioned packages |
| `schemas/` | Shared/public contracts |
| `data/` | Shared data assets where no single owner exists |
| `docs/` | Project-wide documentation |
| `tests/` | Cross-package integration and E2E tests |
| `tools/` | Maintained CLIs, generators, and repo tooling |
| `scripts/` | Thin repo automation |

## Packages

<!-- PACKAGES: to be filled as packages are added -->

## Build & Run

<!-- BUILD COMMANDS: to be filled by agent after language setup -->

## Repo

- **Branch strategy**: {{ branch_strategy }}
{% else %}
## Path Mapping

| Path | Contents |
|------|----------|
| `docs/` | Documentation{% if docs_subdirs %} — `architecture/`, `decisions/` (ADRs), `runbooks/`{% endif %} |
| `tests/` | Integration and E2E tests |
| `scripts/` | Build tooling, automation |

## Build & Run

<!-- BUILD COMMANDS: to be filled by agent after language setup -->

## Repo

- **GitHub**: {{ org }}/{{ project_name }}
- **Branch strategy**: {{ branch_strategy }}
{% endif %}
