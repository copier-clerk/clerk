# rust-toolchain.toml — MANAGED by bailiff-mod-rust (byte-identical on reproduce).
# Pins the Rust toolchain channel. Change rust_channel via copier reproduce to update.
[toolchain]
channel = "{{ rust_channel }}"
