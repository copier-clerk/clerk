[pytest]
# pytest.ini — MANAGED by bailiff-mod-python (byte-identical on every reproduce).
# Edit via `bailiff update`; for deeper customisation migrate to pyproject.toml [tool.pytest.ini_options].
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
