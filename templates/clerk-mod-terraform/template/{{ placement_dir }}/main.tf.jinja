{#-
  main.tf — SEED-ONCE (_skip_if_exists in copier.yml).

  Generic starter. Add provider configurations and resource definitions here.
  Env-per-directory is the idiomatic pattern (no workspaces scaffolded).
-#}
# main.tf — add provider config and resource definitions here.
#
# Example provider configuration:
# provider "aws" {
#   region = var.aws_region
# }
